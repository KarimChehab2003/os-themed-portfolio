import { ProjectData } from "@/lib/types";

function ProjectCard({ name, shortDesc, longDesc, stack, lastModified, live, code, img }: ProjectData) {
    return (
        <div>

        </div>
    );
}

export default ProjectCard;
